; NPM Configuration for Error Page Generator

; Use exact versions (no automatic updates)
save-exact=true

; Fast installation
prefer-offline=true
fetch-timeout=60000

; Publish options
; registry=https://registry.npmjs.org/
; access=public
